import { Schema, model } from "mongoose";
const activitySchema = newSchema({
    name: {
    
        type: String,
        required: true,
    },
    time: {
        type: String,
        required: true,
    },
    notes: [String],

}),
const itinerarySchema = new Schema(
    {
        trip: 
    }
)